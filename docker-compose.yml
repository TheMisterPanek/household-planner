services:
  bot:
    build:
      context: ./ProductTrackerBot
      dockerfile: Dockerfile
    env_file:
      - ./ProductTrackerBot/.env
    environment:
      - DB_PATH=/data/product-tracker.db
    volumes:
      - db-data:/data
    restart: unless-stopped

volumes:
  db-data:
